/* ==========================================================================
   מי אוסף — הגדרות חיבור
   --------------------------------------------------------------------------
   כל עוד שני השדות הראשונים ריקים, האפליקציה עובדת ב־מצב הדגמה: הכול נשמר
   במכשיר בלבד, בלי שרת ובלי חשבון. אחרי שמקימים פרויקט Supabase (ראו README)
   מדביקים כאן את הכתובת ואת המפתח הציבורי — ומאותו רגע האפליקציה עובדת
   מול השרת: כניסה בקוד, סנכרון חי בין כל בני המשפחה, והתראות.
   ========================================================================== */
window.CONFIG = {
  /* מתוך Supabase → Project Settings → API */
  SUPABASE_URL: 'https://oljzujqnotjpmbtnhuwp.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_PVN8yBsTHBQlxP1aakTWQg_Tk6afh1b',

  /* איך נכנסים לאפליקציה:
       'email' — קוד למייל. חינם לגמרי, עובד מהרגע הראשון. (ברירת המחדל)
       'phone' — קוד ב־SMS. נוח יותר לסבים, אבל דורש חיבור ספק הודעות בתשלום. */
  LOGIN: 'email',

  /* מפתח ציבורי ל־Web Push (VAPID). ראו README → התראות. */
  VAPID_PUBLIC_KEY: '',

  /* שם שמופיע בכותרת עד שנטען שם הבית האמיתי */
  APP_NAME: 'מי אוסף',

  /* גרסה — מעלים אותה כדי לאלץ רענון של הקבצים השמורים במכשיר */
  VERSION: '1.0.7'
};

window.CONFIG.isConfigured = !!(window.CONFIG.SUPABASE_URL && window.CONFIG.SUPABASE_ANON_KEY);
